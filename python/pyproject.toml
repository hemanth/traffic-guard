[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "traffic-guard"
version = "0.1.0"
description = "High-throughput traffic and attack defense gate with zero required dependencies and TypeSafe System One acceleration."
readme = "README.md"
authors = [
    { name = "Hemanth HM", email = "hemanth@h3manth.com" }
]
license = { file = "LICENSE" }
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
typesafe = ["typesafe-sdk>=0.6.0"]
test = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "fastapi>=0.110.0",
    "httpx>=0.27.0",
    "typesafe-sdk>=0.6.0",
]

[project.urls]
Home = "https://github.com/hemanth/traffic-guard"
